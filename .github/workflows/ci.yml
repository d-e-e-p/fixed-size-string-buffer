name: CI
run-name: ${{ github.actor }} CI
on: [push]
jobs:
  build-and-test:
    runs-on: ${{ matrix.sys.os }}
    timeout-minutes: 10
    strategy:
      matrix:
        sys:
          - { os: windows-latest, shell: bash }
            #          - { os: ubuntu-latest,  shell: bash }
            #          - { os: macos-latest,   shell: bash }
        configuration:
          - release
    defaults:
      run:
        shell: ${{ matrix.sys.shell }}
    env:
      OS: ${{ matrix.sys.os }}

    steps:
      - run: echo "🎉 job triggered by ${{ github.event_name }} event."
      - run: echo "🐧 running on ${{ runner.os }} server branch ${{ github.ref }} repository  ${{ github.repository }}."
      - name: Checkout Code
        uses: actions/checkout@v3
        with:
          fetch-depth: 1
      - run: echo "💡 ${{ github.repository }} has been cloned to ${{ runner.os }}"
      - name: change encoding to utf8
        if: runner.os == 'Windows'
        shell: pwsh
        run: |
          $OutputEncoding = [console]::InputEncoding = [console]::OutputEncoding = [System.Text.UTF8Encoding]::new()
          Get-ChildItem -Path "." -Recurse | ForEach-Object {
            if ($_.Extension -eq ".h" -or $_.Extension -eq ".cpp") {
                echo "converting filename : $_"
                $content = Get-Content $_.FullName -Encoding UTF8
                Set-Content $_.FullName $content -Encoding UTF8 -Force
            }
          }
          Get-ChildItem -Path "." -Recurse | ForEach-Object {
            if ($_.Extension -eq ".h" -or $_.Extension -eq ".cpp") {
                $content = Get-Content $_.FullName
                echo "filename : $_"
                echo "content : $content"
            }
          }
      - name: List files in the repository
        run: |
          ls '${{ github.workspace }}'
      - run: make ${{ matrix.configuration }}
      - run: make bench
      - run: make test
      - name: Show content of workspace at its completion
        run: ls $RUNNER_WORKSPACE
      - run: echo "🍏 This job's status is ${{ job.status }}."
